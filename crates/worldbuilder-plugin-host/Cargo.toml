[package]
name = "worldbuilder-plugin-host"
version = "0.1.0"
edition = "2021"

[dependencies]
sha2 = "0.10"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
worldbuilder-plugin-api = { path = "../worldbuilder-plugin-api" }
