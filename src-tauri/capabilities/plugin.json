{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "plugin-runtime",
  "description": "Only the host bootstrap and broker commands are exposed to plugin webviews.",
  "windows": ["plugin:*"],
  "permissions": []
}
