# `@worldbuilder/plugin-cli`

The complete plugin authoring, validation, packaging, installation, upgrade,
rollback, and removal guide is [`docs/PLUGIN_SDK.md`](../../docs/PLUGIN_SDK.md).

This package provides the `worldbuilder-plugin` command documented there.
